- Baixou Arquivos "SerialJS" - Marise

- Instalou NODE v8.11.1

- Extrair os arquivos "SerialJS" para um novo Diretorio

- Abrir o gitbash no diretorio selecionado

- Inserir o comando "npm install" para instalar o servidor node

- Abra o diretorio app// entre no arquivo "serial" (abra-o no notepad++ e faça alteraçoes para que o codigo fique
como o"codigo adaptado")

- Inserir o comando "npm start" para iniciar o servidor (arduino tem que estar com o codigo pronto e rodando)

//codigo do servidor (Encontra-se no diretorio)

- Adaptar o codigo da temperatura:

//codigo adaptado

int pinoSensor = 5; // Saída do sensor na A0.
int valorLido = 0; //Variável que armazenará a temperatura lida 
float temperatura = 0;
void setup(){
  Serial.begin(9600);
}
void loop(){
  valorLido = analogRead(pinoSensor);
  temperatura = (valorLido * 0.00488);
  temperatura = temperatura * 100;
  
  Serial.println(temperatura);
  
  delay(1000);
}


